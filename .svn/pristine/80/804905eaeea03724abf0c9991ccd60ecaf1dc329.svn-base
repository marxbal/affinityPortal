import { TestBed, inject } from '@angular/core/testing';

import { DataCouchPouchService } from './data-couch-pouch.service';

describe('DataCouchPouchService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DataCouchPouchService]
    });
  });

  it('should be created', inject([DataCouchPouchService], (service: DataCouchPouchService) => {
    expect(service).toBeTruthy();
  }));
});
